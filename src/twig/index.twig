<!DOCTYPE html>
<html lang="en">
  <head>
    {% include 'header/metatags.twig' with metatags %}
    <!-- The above 3 meta tags *must* come first in the head; any other head content must come *after* these tags -->
    <link rel="shortcut icon" href="img/favicon.ico">
    <title>{{ title }}</title>

    {% include 'header/styles.twig' with styles %}

    <!-- HTML5 shim and Respond.js for IE8 support of HTML5 elements and media queries -->
    <!-- WARNING: Respond.js doesn't work if you view the page via file:// -->
    <!--[if lt IE 9]>
      <script src="https://oss.maxcdn.com/html5shiv/3.7.3/html5shiv.min.js"></script>
      <script src="https://oss.maxcdn.com/respond/1.4.2/respond.min.js"></script>
    <![endif]-->
  </head>
  <body>
    <div id="page">
      {% include 'regions/header.twig' %}
      <main class="content">
        <div class="container">
          {% include 'page/schedule.twig' %}
        </div>
        {% include 'page/components/modal.twig'%}
      </main>
      {% include 'regions/footer.twig' with footer %}
    </div>
    {% include 'header/scripts.twig' with scripts %}
  </body>
</html>
